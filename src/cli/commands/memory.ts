/**
 * cli/commands/memory.ts — shim for backward compatibility.
 * All implementations have moved to cli/commands/memory/ sub-modules.
 */
export { registerMemoryCommands } from "./memory/index.js";
