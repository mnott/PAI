/**
 * cli/commands/registry.ts — shim for backward compatibility.
 * All implementations have moved to cli/commands/registry/ sub-modules.
 */
export { registerRegistryCommands } from "./registry/index.js";
